export default class {
    scene: Number | String;
    widgetId: string;
    zIndex: number;
    hide: boolean;
    constructor({ scene, widgetId, zIndex, hide }: {
        scene: any;
        widgetId: any;
        zIndex: any;
        hide: any;
    });
}
